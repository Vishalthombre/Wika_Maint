<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Register - Global ID</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
<!-- Bootstrap FIRST -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<!-- Your Custom CSS AFTER -->
<link rel="stylesheet" href="/css/register-id.css">
<!-- ✅ Required for PWA -->
  <link rel="manifest" href="/manifest.json">
  <meta name="theme-color" content="#007bff"/>
  <link rel="apple-touch-icon" sizes="180x180" href="/images/image-180x180.jpg">
</head>
<body>

  <!-- Centered Form Card -->
  <div class="d-flex justify-content-center align-items-center min-vh-100 bg-purple">
    <div class="card form-card">
      <div class="card-header">
        <h3>WIKA Maintenance Portal</h3>
        <p style="font-size: 0.9rem;">Enter your Global ID to continue</p>
      </div>

      <div class="card-body">
        <form method="POST" action="/register/check">
          <div class="mb-3">
            <label for="globalId" class="form-label text-white">Global ID</label>
            <input
              type="text"
              id="globalId"
              name="globalId"
              class="form-control"
              value="<%= typeof globalId !== 'undefined' ? globalId : '' %>"
              required
            />
          </div>

          <% if (error) { %>
            <div class="alert alert-danger">
              <%= error %>
            </div>
          <% } %>

          <div class="d-grid">
            <button type="submit" class="btn btn-primary">Continue</button>
          </div>
        </form>
      </div>

      <div class="card-footer">
        Already registered? <a href="/login" class="text-white fw-bold">Login here</a>
      </div>
    </div>
  </div>
  <script src="/js/register-sw.js"></script>
</body>
</html>
